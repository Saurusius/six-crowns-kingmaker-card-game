# Intégration des illustrations — Jeu des Six Couronnes

Cette archive est conçue pour être extraite **à la racine du dépôt du module**.

## Contenu

- `assets/cards/` : 160 dossiers de cartes, chacun avec `full.webp`, `medium.webp` et `thumb.webp`.
- `assets/interface/` : arrière-plans et placeholders d’interface.
- `documentation/` : audit v5, mapping actualisé et manifestes de contrôle.

## Tailles garanties

- `full.webp` : 900 × 1260 px
- `medium.webp` : 450 × 630 px
- `thumb.webp` : 225 × 315 px

Toutes les cartes sont en WEBP statique RGB au ratio 5:7.

## Important

Les anciens dossiers `portraits/`, fichiers `AUDIT.md`, `README.txt`, mappings v4 et listes d’illustrations manquantes ne doivent pas être réintroduits : ils ne correspondent plus à l’état réel du pack.

Le chemin à utiliser dans les données du module est indiqué dans les colonnes `art.full`, `art.medium` et `art.thumb` du fichier `documentation/card-art-mapping-audit-v5.xlsx`.
