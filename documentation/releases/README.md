# Archives des releases

Les notes détaillées sont conservées dans le `CHANGELOG.md` et dans les releases GitHub. Les nouvelles publications doivent être générées avec `npm run prepare-release -- X.Y.Z` afin de garantir une archive reproductible avec `module.json` à la racine.
